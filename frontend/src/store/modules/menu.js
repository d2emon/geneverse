const state = {
  items: [
    {
      icon: 'format_list_bulleted',
      title: 'Nested',
      to: '/generate/multiverse'
    },
    {
      icon: 'work',
      title: 'Challenges',
      to: '/challenges'
    },
    {
      icon: 'star',
      title: 'Space',
      to: '/space'
    },
    {
      icon: 'list',
      title: 'Nested',
      to: '/nested'
    },
    {
      icon: 'account_box',
      title: 'Doll',
      to: '/doll'
    }
  ]
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
